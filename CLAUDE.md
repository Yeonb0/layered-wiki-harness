# 계층형 기관 위키 — 파일럿 하니스

## 프로젝트 배경

권한 층이 존재하는 문서 집합에서 **배치 판정** ( 새 개념을 어느 층에 만들 것인가 ) 과 **하향 조건부 링크**를 수행하는 에이전트를 만들고, 산출 그래프를 **도달성**과 **누출** 두 축으로 평가한다.

최종 산출물은 위키 사이트가 아니라 **평가 결과**다. 코드는 실험을 돌리기 위한 수단이며, 결과물의 완성도보다 측정의 유효성이 항상 우선한다. 사이트 빌드 품질, UI, 문서 렌더링에 시간을 쓰지 마라.

지금 만드는 것은 **실행 순서 3번 ( 파일럿 ) 을 돌리기 위한 하니스**다. 본 에이전트 개발이 아니다.

## 고정 결정 — 대안 설계를 제안하지 마라

| 항목 | 결정 |
| --- | --- |
| 층 구조 | 개인 / 팀 / 전사 3층 고정 |
| 링크 방향 | 아래에서 위로만. 백링크 미기록 |
| 하향 경로 | 조건부 링크. 별도 블록, 가시성 조건을 속성으로 보유 |
| 배포 | 공개 부분만 정적 빌드. 조건부 링크는 로컬 볼트에만 |
| 승격 | 사람이 명시적으로 전환. 에이전트는 후보를 제시하지 않음 |
| 실행 시점 | 문서 투입 즉시 |
| 판정 기록 | 볼트 외부 로그. 실험 조건 · 볼트 크기를 필드로 포함 |
| 권한 범위 | 실험 변수. 전 층 열람 단일 에이전트 / 층별 인스턴스 분리 |
| 모델 | 로컬 서빙 전제. 전 조건 동일 모델 |

이 표의 항목에 대해 대안을 먼저 제안하지 마라. 결정끼리 모순된다고 판단되면 **대안 제시가 아니라 모순 지적만** 하라.

## 용어 고정

층 ( 개인 / 팀 / 전사 ), 상향 링크 / 하향 조건부 링크, 배치 판정, 볼트, 도달성, 누출, 순차 성장, 조건 B0 ( 규칙 기반 ) / B1 ( 단일 프롬프트 ) / B2 ( 기존 오픈소스 위키 빌더 ) / 제안.

**"권한 관리", "접근 제어" 라는 표현은 코드 · 주석 · 문서 어디에도 쓰지 마라.** 이 프로젝트는 접근 제어 모델을 기여로 주장하지 않는다. 다루는 질문은 "이 사용자에게 무엇을 보여줄 것인가" 가 아니라 "이 연결을 만들어도 되는가, 만든다면 어느 층에" 다.

## 범위

### 안
파일럿 하니스 — 검색 레이어, 순차 성장 러너, Dify 워크플로 호출, 판정 로그 writer, 집계 스크립트.

### 밖 — 슬며시 들어오면 즉시 지적하라
- 승격 처리 ( 설계는 고정해 두되 구현하지 않는다 )
- 다중 소스 자동 수집
- 긴 문서의 개념 단위 분해
- 실시간 협업 편집
- **인증 · 세션 배관.** "인스턴스마다 보이는 문서 집합이 다르다" 는 검색 계층의 필터만으로 충족한다
- 위키 사이트 빌드, UI, 렌더링

## 만들 것

Dify 워크플로 자체는 사람이 Dify UI 에서 구성한다. 네가 만드는 것은 그 **바깥 하니스**다.

### 1. 검색 레이어 ( retrieval/ )
전 조건이 **동일한 검색 레이어를 공유**해야 한다. 이 등가성을 깨는 최적화는 제안하지 마라.
- llama-server 임베딩 엔드포인트 http://localhost:8081/v1/embeddings ( bge-m3 )
- search(query, k, mode) -> list[Candidate]
- mode 는 all_layers | split_by_layer. split_by_layer 는 후보 목록을 해당 층 이하로 필터링하는 것으로만 구현한다. 인증이나 세션을 붙이지 마라
- Candidate 형식 : { "id", "layer", "title", "excerpt" }
- k 는 호출 인자로 노출한다. 파일럿에서 4 / 8 / 16 을 비교한다
- **하이브리드 검색은 구현하지 마라.** 구현 세부가 미결정이다. 인터페이스만 남겨라

### 2. 볼트 ( vault/ )
- 볼트 파일 포맷은 **미결정**이다. 파일럿에서는 JSONL 로 잠정 처리하고 포맷 의존 코드를 한 모듈에 격리하라. 확정 사항처럼 문서화하지 마라
- 문서 레코드 : { "id", "layer", "title", "body", "upward_links", "downward_conditional_links", "inserted_at_vault_size" }
- 하향 조건부 링크는 별도 블록으로 저장하고 가시성 조건을 속성으로 가진다
- 백링크는 기록하지 않는다

### 3. 순차 성장 러너 ( runner/ )
- 빈 볼트에서 시작해 문서를 한 건씩 무작위 순서로 투입한다
- 매 투입마다 : 검색 -> 판정 호출 -> 검증 -> 볼트 갱신 -> 로그 append
- 결과는 단일 수치가 아니라 **볼트 크기에 대한 곡선**이다
- **조건 인터리빙 필수.** 조건을 블록으로 몰아 돌리지 마라. 건 단위로 섞는다. 노트북 CPU 의 발열 드리프트가 볼트 크기 축과 공선형이 되기 때문이다
- 콜드 스타트 감지 : 직전 호출로부터 경과 시간이 임계값을 넘으면 cold_start 를 세운다

### 4. Dify 호출 ( client/ )
- Dify 워크플로 실행 API. 엔드포인트와 API 키는 환경변수
- 입력 5개 : Doc, Candidates ( JSON 문자열 ), VaultSize, Mode, RunConfig ( JSON 문자열 )
- 출력 4개 : ok, errors, verdict, log
- 타임아웃과 재시도를 두되, 재시도한 건은 retried 플래그로 표시한다

### 5. 판정 로그 ( logging/ )
**판정 로그는 편의 기능이 아니라 평가 인프라다.** 볼트 외부에 JSONL 로 append 한다.
판정마다 : 조회된 후보 목록 전체 ( excerpt 포함 ), 선택 근거, 볼트 크기, 실험 조건, 권한 모드, RunConfig 스냅샷, cold_start, retried, wall_clock, run_index, 처리 시간, 모델 raw 출력 원문.
**사후에 지표를 바꿔도 재실행 없이 재집계할 수 있어야 한다.**

### 6. 집계 스크립트 ( analysis/ )
- 로그 JSONL 만 읽는다. 볼트나 모델에 접근하지 않는다
- **평균보다 분산을 먼저 낸다**
- 처리 시간 집계에서 cold_start 건과 retried 건을 제외한다
- **절대 목표 수치를 어디에도 걸지 마라.** 통과 · 실패 판정 로직을 넣지 마라
- 누출 집계는 모드 간 대조다. 전 층 열람 모드와 층별 분리 모드에서 생성된 상위 층 항목을 비교해, 하위 층 문서에만 등장하던 고유 표현 ( 고유명사 · 내부 용어 · 수치 ) 이 상위 항목에 나타난 건수를 센다. 층별 분리 모드는 정의상 누출 0 인 하한 기준선이다

## 실행 환경

- Omarchy ( Arch 계열 ), 외장 HDD 의 LUKS btrfs 위. 디스크가 느리다는 전제로 불필요한 파일 IO 를 만들지 마라
- RAM 32GB, RTX 3050 **4GB**
- 판정 LLM : Qwen3 8B Q4_K_M, **CPU 전량 추론** ( -ngl 0 ), llama-server :8080, OpenAI 호환
- 임베딩 : bge-m3, **GPU 상주** ( -ngl 99 ), llama-server :8081
- num_ctx 8192, num_predict 1024, temperature 0, seed 42, threads = 물리 코어 - 2
- Dify 는 같은 머신의 Docker. 컨테이너와 추론 스레드가 CPU 를 두고 경합하므로 하니스가 CPU 를 많이 쓰는 구조를 만들지 마라

## 절대 하지 말 것

1. **배치 판정 프롬프트의 층 정의를 임의로 작성하지 마라.** 층 정답 라벨링 기준이 아직 성문화되지 않았다. 네가 그럴듯한 기준을 채워 넣으면 판정 기준과 정답 라벨의 기준이 어긋나 실험 전체가 무효가 된다
2. **미결정 항목의 값을 지어내지 마라.** 본실험 문서 건수, 라벨링 표본 크기, 인간 평가 표본 크기, 하이브리드 검색 세부, 볼트 파일 포맷 — 필요하면 **묻고 멈춰라**
3. 검색 레이어를 조건별로 다르게 만들지 마라
4. 실제 인증 · 세션을 붙이지 마라
5. 절대 목표 수치나 통과 기준을 넣지 마라
6. 단일 층으로 먼저 만들자, 권한 모드를 하나만 구현하자는 방향은 기여의 절반을 문서로만 남긴다. 제안하지 마라

## 코드 규약

- 들여쓰기 2칸
- 배열 · 자료구조 변수명은 첫 글자 대문자
- 주석 끝에 마침표를 붙이지 않는다 ( 소수점, 버전 번호, 절 번호는 예외 )
- 파일 전체 재작성은 명시적으로 요청받았을 때만 한다. 수정은 변경 지점을 줄 번호와 함께 보여라

## 진행 방식

전체를 한 번에 쓰지 마라. 다음 순서로 진행하고 각 단계마다 멈춰서 확인을 받아라.

1. 디렉터리 구조와 모듈 경계, 인터페이스 시그니처만 제시
2. 판정 로그 스키마 확정 — 나머지를 규정하므로 먼저 굳힌다
3. 검색 레이어
4. Dify 클라이언트 + 응답 검증
5. 순차 성장 러너
6. 집계 스크립트
